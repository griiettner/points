/**
 * Fallback Component
 *
 * A simple component that displays a loading message. It can be used as a placeholder
 * or a fallback UI while waiting for data or when rendering is temporarily suspended.
 *
 * @module components/Fallback
 * @returns {JSX.Element} A div element with a loading message.
 */

export const Fallback = () => {
  return <div>Loading...</div>;
};
